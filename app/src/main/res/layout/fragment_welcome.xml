<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Imagen de fondo -->
    <ImageView
        android:id="@+id/fondoImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/fondo_app"
        android:scaleType="centerCrop"
        android:contentDescription="Fondo de la app" />

    <!-- Overlay con gradiente oscuro -->

    <!-- Logo grande arriba centrado -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_gradient" />

    <ImageView
        android:id="@+id/logoImage"
        android:layout_width="358dp"
        android:layout_height="452dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:contentDescription="Dulce Horno"
        android:scaleType="centerInside"
        android:src="@drawable/logo_app" />

    <!-- Botón de Iniciar Sesión abajo -->

    <!-- Texto "No tienes una cuenta" debajo del botón -->

    <Button
        android:id="@+id/buttonLogin"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="40dp"
        android:layout_marginEnd="40dp"
        android:layout_marginBottom="169dp"
        android:backgroundTint="#8B4513"
        android:text="Iniciar Sesión"
        android:textColor="#FFFFFF"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp">

        <TextView
            android:id="@+id/textNoCuenta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="¿No tienes una "
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/textCuenta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="cuenta?"
            android:textColor="#FF0000"
            android:textStyle="bold"
            android:textSize="14sp"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>

</RelativeLayout>
